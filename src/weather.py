import flet

from flet import (
    Control, Column, Container, IconButton, Page, Row, Text, TextButton, IconButton, ButtonStyle, PopupMenuButton,
    PopupMenuItem, TextField, border_radius, colors, icons, padding, border
    )


class WeatherPage(Control):
